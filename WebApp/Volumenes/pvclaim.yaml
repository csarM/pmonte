apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim
spec:
  storageClassName: systemfiles
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
